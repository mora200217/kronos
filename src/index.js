import React, { Component } from 'react'
import { render } from 'react-dom'
import App from './App.js'
import 'bootstrap';

// Get container from DOM
const appContainer = document.getElementById('appContainer');
render(<App />, appContainer); // ReactDOM.react
